# 1. Create a new PyCharm project: Exercise9.
# 2. AddanewPythonfiletotheprojectcalledtypes.py
# 3. Withinthisfile,createtwovariables:onecontainingyourfirstnameandanother containing your last name.
# 4. Usetheprintfunctiontooutputyourfullnamewithaspacebetweenyourfirst and last name.
# 5. Nowtransferthesevariablevaluesintoalistanddisplaythelist.
# 6. Take the variables and now store the values in a dictionary, using the keys 'first'
# and 'last'. Display the dictionary values.

firstname = "Pri"
lastname = "Adom"

print(firstname, lastname)

myfullname = ["Pri","P.","Adom"]

print(myfullname[0],myfullname[2])

# having issues with git, seems like everytime I want to add a new project, I need to create a new repository?
# managed to upload work on git hub which is a success
# find folder you're git is connected to using terminal: cd (check directory), ls (check list), cd .. to go back cd ., cd ../., cd../..
# next git clone with link to github repository
# git add ., git commit -m "name or description"
# git status
# git push
mydict = {"first":"Pri", "last":"Adom"}

print(mydict["first"], mydict["last"])
